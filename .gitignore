# Dependencies
node_modules/
.pnp
.pnp.js

# Environment Variables (NEVER COMMIT SECRETS)
# .env — allowed to commit if it only contains public-safe vars (SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY)
# Uncomment the line below if you ever add secrets back to .env:
# .env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# Cloudflare / Wrangler residue
.wrangler/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS Files
.DS_Store
Thumbs.db

# Build outputs & cache
dist/
build/
.cache/
coverage/
